
export const registerApolloMock = (client) => {
    // This is where you can register mock data for development
    // For example, using the apollo-mock-client package
    console.log('Apollo mock registered with client', client);
  };